package com.example.weather_report.utils

enum class LocationOptions(val optName: String, val desc: String) {
    GPS("GPS", "Determine Location Via GPS"),
    MAP("Map", "Determine Location Via Map");
}